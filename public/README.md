# Public Web Root

Future web-facing files should live here once the cdmon document root can be
pointed to this directory.

For now, the production endpoint remains at the repository root in `index.php`.
