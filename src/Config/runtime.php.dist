<?php

declare(strict_types=1);

/**
 * Plantilla de configuración runtime.
 *
 * Copia este fichero a src/Config/runtime.php y rellena con los valores
 * reales del entorno. src/Config/runtime.php NO se versiona (está en .gitignore)
 * y NO se sube por el workflow de despliegue.
 *
 * Cada entorno (local, producción) mantiene su propio runtime.php con sus secretos.
 */

return [
    // ===== Aplicación =====
    'APP_ENV' => 'production',
    'APP_URL' => 'https://logs.singularthings.io',

    // ===== Ingesta de bridges =====
    'LOG_INGEST_SECRET'     => 'CHANGE_ME',
    'LOG_INGEST_USER_AGENT' => 'WalkerPisa-Bridge-Logs',

    // ===== Base de datos =====
    'DB_HOST'     => 'localhost',
    'DB_PORT'     => '3306',
    'DB_DATABASE' => 'logs-devices',
    'DB_USERNAME' => 'CHANGE_ME',
    'DB_PASSWORD' => 'CHANGE_ME',

    // ===== Viewer (HTTP Basic Auth) =====
    'VIEWER_AUTH_ENABLED'  => 'true',
    'VIEWER_AUTH_USERNAME' => 'admin',
    'VIEWER_AUTH_PASSWORD' => 'CHANGE_ME',

    // ===== Endpoints de administración (desactivados por defecto) =====
    'REPROCESS_ENABLED'           => 'false',
    'ADMIN_REPROCESS_TOKEN'       => 'CHANGE_ME',
    'ARCHIVE_RAW_ENDPOINT_ENABLED' => 'false',

    // ===== Rate limiting de ingesta =====
    'INGEST_RATE_LIMIT_ENABLED'         => 'true',
    'INGEST_RATE_LIMIT_MAX'             => '1000',
    'INGEST_RATE_LIMIT_WINDOW_SECONDS'  => '600',

    // ===== Storage =====
];
